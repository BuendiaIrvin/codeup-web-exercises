"use strict";


// Define a function named allIndexesOf that takes in two arguments. The first argument should be the array to
// search and the second argument should be the value you want to search for. If the item does not exist in
// the provided array, return an empty array.

var fruits = ["apple", "banana", "orange", "apple", "pineapple"];
var array = fruits
// allIndexesOf(fruits, "apple") should return the array [0, 3]
// allIndexesOf(fruits, "guava") should return the array []
// allIndexesOf(fruits, "pineapple") should return [4]

function allIndexesOf (array, value) {
   return ;
}
console.log(allIndexesOf(fruits, "banana"));